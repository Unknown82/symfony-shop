<header class="bg-white shadow-md sticky top-0 z-50">
    <div class="container mx-auto px-4 flex items-center justify-between">
        <a href="" class="text-2xl font-bold text-blue-600">Symfony-Shop</a>

        <button id="menu-toggle" class="md:hidden text-gray-600 focus:outline-none">
            ≡
        </button>
        <div class="hidden md:flex items-center space-x-8">
            <nav class="flex space-x-6 text-gray-700 font-medium">
                <a href="{{ path('app_home') }}" class="hover:text-blue-600 transition">Produkte</a>
                <a href="{{ path('app_home') }}" class="hover:text-blue-600 transition">Startseite</a>
            </nav>
            <div class="flex items-center space-x-4">
                {% if app.user %}
                    <a href="{{ path('cart_view') }}" class="relative to-blue-600 hover:to-blue-800">
                        Cart 🛒
                        <span class="absolute -top-2 -right-3 bg-red-500 text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center shadow">
                            <span id="cart-count"> {{ count ?? 0 }} </span>
                        </span>
                    </a>
                    <a href="{{ path('app_customer') }}" class="text-sm px-4 py-2 rounded-lg bg-green-500 text-white hover:bg-green-600 transition">Dashboard</a>
                    <a href="{{ path('app_logout') }}" class="text-sm px-4 py-2 rounded-lg bg-red-500 text-white hover:bg-red-600 transition">Abmelden</a>

                {% else %}
                    <a href="{{ path('app_login') }}" class="text-sm px-4 py-2 rounded-lg border border-blue-500 text-blue-500 hover:bg-blue-50 transition">Anmelden</a>
                    <a href="{{ path('app_register') }}" class="text-sm px-4 py-2 rounded-lg bg-blue-500 text-white hover:bg-blue-600 transition">Registrieren</a>
                {% endif %}
            </div>

        </div>
    </div>
</header>
